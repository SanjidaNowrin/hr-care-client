

function App() {
  // const dispatch = useDispatch();
  // useEffect(() => {
  //   dispatch(fetchBooks());
  // }, []);
  // const books = useSelector((state) => state.books.discover)
  return (
    <>

      <h2>HR Care</h2>

    </>
  );
}

export default App;
